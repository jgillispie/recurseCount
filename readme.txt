
recurseCount.py counts directories and files contained within a directory.  It is set up to run from command line.
Usage: python recurseCount.py <dir>

	<dir> - The pathname to count files and directories from.